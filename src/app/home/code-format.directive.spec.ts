import { CodeFormatDirective } from './code-format.directive';

describe('CodeFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CodeFormatDirective();
    expect(directive).toBeTruthy();
  });
});
